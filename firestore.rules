rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ------------------------------------
    // HELPERS
    // ------------------------------------
    function signedIn() {
      return request.auth != null;
    }

    function userRole() {
  return exists(/databases/$(database)/documents/users/$(request.auth.uid))
    ? get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role
    : null;
}

    function isAdmin() {
      return userRole() == "admin";
    }

    function isAgent() {
      return userRole() == "subuser";
    }

    // Helper to get assigned agent for a lead, handling both field naming conventions
    function getLeadAssignedTo(leadId) {
      let lead = get(/databases/$(database)/documents/leads/$(leadId));
      return lead.data.assignedTo || lead.data.assigned_to;
    }

    // ------------------------------------
    // ADMIN NOTIFICATIONS
    // ------------------------------------
    match /admin_notifications/{id} {
      // Admin can view notifications
      allow list, read: if signedIn() && isAdmin();

      // Admin can ONLY update the `read` field
      allow update: if signedIn()
        && isAdmin()
        && request.resource.data.diff(resource.data).changedKeys().hasOnly(['read']);

      // Creation only via Cloud Functions (Admin SDK bypasses rules)
      allow create, delete: if false;
    }

    // ------------------------------------
    // LEADS
    // ------------------------------------
    match /leads/{leadId} {

  // LIST queries (collection reads)
  allow list: if signedIn() && (
    isAdmin() ||
    isAgent() 
  );

  // GET a single lead
  allow get: if signedIn() && (
    // Admin can read all
    isAdmin()

    // Assigned user can read (array-based)
    || (
      resource.data.assignedTo is list
      && resource.data.assignedTo.hasAny([request.auth.uid])
    )

    // ✅ Safe read during transfer lifecycle
    // Prevents PERMISSION_DENIED on active listeners
    || (
      request.auth != null
      && request.time < request.resource.updateTime
    )
  );

  // CREATE
  allow create: if isAdmin() || isAgent();

  // UPDATE
  allow update: if
    // ✅ Admin: full control
    isAdmin()

    // ✅ Assigned subuser: limited safe updates (status, follow-up, remarks)
    || (
      isAgent()
      && resource.data.assignedTo is list
      && resource.data.assignedTo.hasAny([request.auth.uid])
      && request.resource.data.diff(resource.data).changedKeys().hasOnly([
        "status",
        "tag",
        "updatedAt",
        "statusChangedAt",
        "followUpDate",
        "followUpTime",
        "remarks",
        "companyName",
        "clientEmail",
        "requirement"
      ])
    )

    // ✅ Subuser TRANSFER (REPLACE assignedTo with ONE user)
    || (
      isAgent()
      && request.resource.data.diff(resource.data).changedKeys()
          .hasOnly(["assignedTo", "updatedAt"])
      && request.resource.data.assignedTo is list
      && request.resource.data.assignedTo.size() == 1
    );

  // DELETE only admin
  allow delete: if isAdmin();

    // 🔹 Subcollection notes under a lead
    match /notes/{noteId} {
      // 🟢 READ:
      // Agents can read notes if they are assigned to this lead
      // Admins can read all
      allow read: if signedIn() &&
        (
          isAdmin()
          || (
            get(/databases/$(database)/documents/leads/$(leadId)).data.assignedTo is list
            && get(/databases/$(database)/documents/leads/$(leadId)).data.assignedTo.hasAny([request.auth.uid])
          )
        );

      // 🟢 CREATE:
      // Agent can create a note if:
      //   - They are assigned to this lead
      //   - "by" equals their uid
      //   - "text" exists and is a string
      allow create: if signedIn() &&
        (
          isAdmin()
          || (
            get(/databases/$(database)/documents/leads/$(leadId)).data.assignedTo is list
            && get(/databases/$(database)/documents/leads/$(leadId)).data.assignedTo.hasAny([request.auth.uid])
          )
        )
        && request.resource.data.keys().hasAll(['text', 'createdAt', 'by'])
        && request.resource.data.by == request.auth.uid
        && request.resource.data.text is string
        && request.resource.data.text.size() > 0;

      // 🛑 UPDATE & DELETE:
      // Only admin can edit/delete notes
      allow update, delete: if isAdmin();
    }

    // ------------------------------------
    // LEGACY / TOP-LEVEL lead_notes collection
    // ------------------------------------
    match /lead_notes/{noteId} {
      // Read allowed for admins or the agent assigned to that lead
      allow read: if signedIn() && (
        isAdmin() ||
        // resource available on read; check the referenced lead's assigned agent
        (resource.data.lead_id != null && (
          get(/databases/$(database)/documents/leads/$(resource.data.lead_id)).data.assignedTo == request.auth.uid ||
          get(/databases/$(database)/documents/leads/$(resource.data.lead_id)).data.assigned_to == request.auth.uid
        ))
      );

      // Admins can manage all
      allow create, update, delete: if isAdmin();

      // Agents can create notes for leads they are assigned to. Accepts `{ lead_id, sender_id, message }` schema.
      allow create: if signedIn() && (
        resource.data.lead_id is string &&
        (get(/databases/$(database)/documents/leads/$(resource.data.lead_id)).data.assignedTo == request.auth.uid ||
         get(/databases/$(database)/documents/leads/$(resource.data.lead_id)).data.assigned_to == request.auth.uid)
        && request.resource.data.keys().hasAll(['lead_id','sender_id','message'])
        && request.resource.data.sender_id == request.auth.uid
        && request.resource.data.message is string
        && request.resource.data.message.size() > 0
      );

      // Agents may update their own legacy notes (only message field)
      allow update: if signedIn() && resource.data.sender_id == request.auth.uid && request.resource.data.keys().hasOnly(['message']);
    }
  }

    // ------------------------------------
    // LEAD HISTORY (ADMIN + ACTOR)
    // ------------------------------------
    match /leads/{leadId}/history/{historyId} {
      allow read: if
        isAdmin()
        || (
          request.auth != null
          && resource.data.hiddenForUid == request.auth.uid
        );

      allow create: if signedIn()
        && (
          isAdmin()
          || request.resource.data.by == request.auth.uid
        );

      allow update, delete: if false;
    }
    // ------------------------------------
    // LEAD STATUSES
    // ------------------------------------
    match /lead_statuses/{statusId} {
      // Allow read if user is authenticated
      allow read: if signedIn();
      
      // Allow write only for admin users
      allow write: if isAdmin();
    }

    // ------------------------------------
    // ATTENDANCE
    // ------------------------------------
    match /attendance/{attendanceId} {
  allow list: if signedIn();
  // 🔍 Read - Only admin or owner can read attendance
  allow read: if signedIn() && (
    isAdmin()
    || resource.data.userId == request.auth.uid
    || resource.data.user_id == request.auth.uid
  );

  // ➕ Create
  // ✅ Admin can create attendance for ANY user (manual attendance from CRM)
  // ✅ Agent can create ONLY for self (support both user_id and userId fields)
  allow create: if signedIn()
    && (
      // Admin can create for anyone
      isAdmin()

      // User can create only for self (support both field names)
      || request.resource.data.userId == request.auth.uid
      || request.resource.data.user_id == request.auth.uid
    )
    && request.resource.data.record_type in ["attendance","absent", "leave"]

    // Attendance-specific validation
    && (
      request.resource.data.attendance_status == "present"
        ? (
            request.resource.data.punchInTime is timestamp
            || request.resource.data.punch_in_time is timestamp
          )
        : true
    )

    // Leave-specific validation
    && (
      request.resource.data.record_type == "leave"
        ? (
            request.resource.data.start_date is timestamp
            && request.resource.data.end_date is timestamp
          )
        : true
    );

  // ✏️ Update
  allow update: if
    // ✅ Admin full control
    isAdmin()

    // ✅ Agent punch-in / punch-out updates ONLY
    || (
      (
        resource.data.userId == request.auth.uid
        || resource.data.user_id == request.auth.uid
      )
      && request.resource.data.diff(resource.data).changedKeys().hasOnly([
        "punch_out_time",
        "punch_out_location",
        "punch_out_address",
        "updated_at",
        "start_date",
        "end_date"
      ])
      && (!('start_date' in request.resource.data) || request.resource.data.start_date is timestamp)
      && (!('end_date' in request.resource.data) || request.resource.data.end_date is timestamp)
    );

  // ❌ Delete (Admin only)
  allow delete: if isAdmin();
}

    // ------------------------------------
    // USERS
    // ------------------------------------
    match /users/{userId} {
      // Anyone logged in can read users (needed for assignedTo names)
      allow read: if signedIn();

      // Admin full control
      allow create, delete, update: if isAdmin();

      // Users can update ONLY their own safe profile fields
      allow update: if signedIn()
        && request.auth.uid == userId
        && request.resource.data.diff(resource.data).changedKeys().hasOnly([
          "name",
          "phone",
          "gender",
          "dateOfBirth",
          "address",
          "active",
          "updatedAt",
          "fcmToken" // ✅ ADD THIS ONLY
        ]);

      // NOTE: agent/stat fields (assignedLeads, closedDeals, etc.) must NOT be writable by clients.
      // Those fields should be maintained by trusted server code (Cloud Functions) using the Admin SDK.
    }

    // ------------------------------------
    // USER NOTIFICATIONS (SUB-USER APP)
    // ------------------------------------
    match /users/{userId}/notifications/{id} {
      allow list: if signedIn() && request.auth.uid == userId;
      allow read: if signedIn() && request.auth.uid == userId;
      allow write: if false; // only Cloud Functions
    }

    // ------------------------------------
    // LEAD SOURCES
    // ------------------------------------
    match /lead_sources/{docId} {
      allow read: if signedIn();     // Needed for dropdowns
      allow write: if isAdmin();
    }

    // ------------------------------------
    // LEAD PURPOSES
    // ------------------------------------
    match /lead_purposes/{docId} {
      allow read: if signedIn();
      allow write: if isAdmin();
    }

    // ------------------------------------
    // INVOICES
    // ------------------------------------
    match /invoices/{docId} {
      allow read: if isAdmin();
      allow write: if isAdmin();
    }

    // ------------------------------------
    // BANK DETAILS
    // ------------------------------------
    match /bankDetails/{docId} {
      allow read, write: if isAdmin();
    }

    // ------------------------------------
    // ADMIN SETTINGS
    // ------------------------------------
    match /admin/{docId} {
      allow read, write: if isAdmin();
    }

    // ------------------------------------
    // DENY EVERYTHING ELSE
    // ------------------------------------
    match /{path=**} {
      allow read, write: if false;
    }
  }
}